steps:

  - type: helm
    environments:

      cloud:
        config:
          repoName: cloudflare
          repoUrl: https://cloudflare.github.io/helm-charts
          chartName: cloudflare/cloudflared
          version: 0.3.2
          releaseName: cloudflared
          namespace: cloudflare
          values:
            - environments/cloud/cloudflared.values.yaml
      